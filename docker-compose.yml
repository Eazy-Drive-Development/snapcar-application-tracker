version: '3.9'

services:
  backend:
    build: ./backend
    ports:
      - '4000:4000'
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - '5173:80'
    env_file:
      - ./frontend/.env
    restart: unless-stopped
